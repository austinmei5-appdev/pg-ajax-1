var new_comment = $("<%= j(render 'comments/comment', comment: @comment) %>");

new_comment.hide();

$("#<%= dom_id(@comment.photo) %>_new_comment_form").before(new_comment);

new_comment.slideDown();

$("#<%= dom_id(@comment.photo) %>_new_comment_form #comment_body").val("");
